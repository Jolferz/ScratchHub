{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<div class="col-sm-12 main center-me" >
    <!-- resumt -->
    <div class="panel panel-default">
        <div class="panel-heading resume-heading">
            <div class="row">
                <div class="col-lg-12">
                    <div class="col-md-10 col-lg-4 col-md-offset-1">
                        {{{ iframe }}}
                    </div>
                    <div class="col-md-12  vertical-center ">
                        <ul class="list-group">
                            <li class="list-group-item"><strong>Name:</strong> {{ name }}</li>
                            <li class="list-group-item"><strong>Category:</strong> {{ category }}</li>
                            <li class="list-group-item"><strong>Developed by:</strong><a href="/profile/{{ authorLink }}"> {{ authorLink }}</a></li>
                        </ul>
                    </div>
                </div>
                {{#if modBtns}}
                    <div id="delete-btn">
                        <button id="project-delete-btn">Delete</button>
                    </div>
                    <div id="edit-btn">
                        <a href="/project/{{ name }}/update-project"><button>Edit</button></a>
                    </div>
                {{/if}}
            </div>
        </div>
        <div class="bs-callout bs-callout-danger project-description">
            <h4>Description</h4>
            <p>
                {{ description }}
            </p>
        </div>               
    </div>
</div>


<!-- Comments area -->
<div class="container">
    <div class="row">
        <div class="col-md-10 col-centered">
            {{#each comments}}
                <div class="widget-area no-padding blank">
                    <div class="comment-user" ><i class="fa fa-user"></i><a href="/profile/{{ this.commenter.username }}">{{ this.commenter.username }}</a></div>
                    {{#if commentDltBtn }}
                        <div id="delete-btn">
                            <button id="project-delete-btn">Delete</button>
                        </div>
                    {{/if}}
                    <div class="msg-box">
                        <p>{{ this.comment }}</p>
                    </div>
                    <span class="comment date">{{ this.postDate }}</span>   
                </div>
            {{/each}}
        </div>
    </div>
</div>

<!-- Comments form -->
<div class="container">
    <div class="row">
        <div class="col-md-10 col-centered">
            <div class="widget-area no-padding blank">
                <div class="status-upload">
                    <fieldset>
                        <form method="post" action="/project/{{ name }}/comment">
                            <textarea type="text" class="form-control" name="comment" placeholder="Say something nice..." ></textarea>                
                            <button type="submit" class="btn btn-success blue">Post</button>
                        </form>
                    </fieldset>
                </div>
            </div>
        </div>
    </div>
</div>

